// Intentionally empty. This wrapper target bundles the JRE and JSR305 binary
// xcframeworks into a single consumable SPM product and carries the linker
// settings the J2ObjC runtime needs (iconv, z, and system frameworks).
